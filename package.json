{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^16.0.2",
    "purescript": "^0.15.15",
    "purescript-psa": "^0.9.0",
    "purs-tidy": "^0.11.0",
    "rimraf": "^6.0.1"
  }
}
